name: 'Tag'
description: 'Create a git tag'
inputs:
  tag:
    description: 'The git tag name'
  sha:
    description: 'The commit SHA to tag'
  user:
    description: 'The tag user name'
  email:
    description: 'The tag email address'
  message:
    description: 'The tag message'
runs:
  using: 'node16'
  main: 'index.js'
